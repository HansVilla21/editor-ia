/**
 * El estilo neutro: un punto de partida que se ve bien apenas instalás.
 *
 * No es el estilo definitivo de nadie. Cuando entrenes el tuyo con la skill
 * `estudiar-referentes`, estos valores se reemplazan por los que salgan de tus
 * propias referencias, y los colores por los de tu skill `mi-marca`.
 */
export const colores = {
  fondo: "#0B0F16",
  fondoPanel: "#151B2B",
  texto: "#FFFFFF",
  textoSuave: "#A3ADBA",
  acento: "#5B8DEF",
};

export const tipografia = {
  familia: "system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  pesoTitular: 800,
  pesoCuerpo: 500,
};

/** Subtítulos: legibles sobre cualquier fondo, sin tapar la cara. */
export const subtitulos = {
  tamano: 60,
  peso: 800,
  espaciado: "-0.02em",
  sombra: "0 4px 24px rgba(0,0,0,0.65)",
  /** Distancia desde abajo, en porcentaje de la altura. */
  desdeAbajo: 0.28,
};
